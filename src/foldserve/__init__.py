"""FoldServe — distributed ESM-2 protein-model deployment on a GPU cluster.

Lifecycle: distributed fine-tune -> multi-GPU serving -> cross-cloud
orchestration -> deploy into a customer VPC with data staying in-region.
"""

__version__ = "0.1.0"
