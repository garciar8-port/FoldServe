# FoldServe

Distributed protein-model deployment on a GPU cluster — fine-tune ESM-2, serve multi-GPU, orchestrate across clouds, deploy into a customer VPC.

Project brief (thinking/context) lives in the Obsidian vault: `Personal Projects/FoldServe/`.
